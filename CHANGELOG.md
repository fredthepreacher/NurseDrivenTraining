# Nurse Driven Training — Changelog

## Hero background video + Programs page hands-on photo

- **Homepage hero video**: added the supplied classroom video as a looping, muted, autoplaying background clip in the hero's existing media panel (next to the headline/CTA, not full-bleed behind the text). Replaced the static classroom photo that was there before.
- **Important finding before implementing**: the supplied video is actually vertical phone footage (portrait) that had been padded into a 1280×720 landscape frame with a blurred, zoomed copy of itself filling the left/right sides — not genuine widescreen footage. Using it as a literal full-width cinematic background (as a generic spec would assume) would have shown mostly soft blur on both sides. Flagged this to the owner, who chose to crop tight into the real footage. Re-encoded the video cropped to just the sharp content (400×720, confirmed via a frame-analysis script measuring image detail column-by-column to find the exact real/blurred boundary), which also shrank the file from ~1.5MB to about 630KB.
- Given the real footage is portrait-shaped, and the site's actual hero layout is a two-column design (text + a contained media panel) rather than a full-bleed background, kept the video in that existing panel at its natural portrait proportions instead of forcing a full-width background-video layout — this matches the source material, avoids the text-readability/overlay complexity of a full-bleed treatment, and follows the "don't rewrite the hero unless necessary" guidance in the request.
- Video is muted, loops, autoplays, uses `playsinline`, `preload="metadata"`, `aria-hidden="true"`, and `tabindex="-1"` (fully decorative, not keyboard-focusable, no captions needed since no audio track exists).
- Added a poster image (`nurse-driven-training-hero-poster.jpg`/`.webp`, extracted from the cropped video, ~400×720, 24-44KB) as the `poster` attribute and as a real `<img>` fallback with proper alt text.
- Added `@media (prefers-reduced-motion: reduce)` support: the video is hidden and the static poster photo is shown instead for users who've asked for reduced motion, so the same visual content is available either way.
- Files added: `assets/video/nurse-driven-training-hero-background.mp4`, `assets/img/nurse-driven-training-hero-poster.jpg`, `assets/img/nurse-driven-training-hero-poster.webp`.
- **Not performed this round (no tool access in this environment)**: a real Lighthouse run, cross-browser autoplay testing (Safari/WebKit in particular is stricter about autoplay), and network throttling tests. The video is small (~630KB) and `preload="metadata"` avoids buffering the full file upfront, but real-device testing on the deployed site is recommended before considering this fully verified.
- **Programs page**: added the existing infant-CPR hands-on training photo in a new "Across Every Program — Hands-on skills practice, not just lecture" section, tying together all three programs (CNA/HHA/CPR) around the shared hands-on-practice theme. This photo already appears on the HHA and CPR pages in program-specific contexts; this placement is intentionally different (a summary/overview page, different heading and copy), consistent with how the site has handled this photo's reuse elsewhere.

## Fix: About page "Meet the Team" photo cropping heads

- **Root cause**: the team-welcome photo is a portrait-oriented image (1023×1538, ~2:3) but was displayed in `.photo-frame.wide`, a 16:9 landscape container, with the sitewide `.photo-frame img { object-fit: cover }` rule. That combination forced the image to fill a container far wider (relative to its height) than the photo itself, so `cover` scaled it up and cropped roughly a third off the top and a third off the bottom to fill the frame — cutting off both women's heads and hair.
- **Fix**: gave this specific image an inline `aspect-ratio: 2/3` (matching its native ratio almost exactly) instead of the `wide` (16:9) class. At that ratio the crop needed to fill the frame is under 0.3% of the image — visually identical to showing the full photo — so both heads, hands, faces, and the entrance sign are fully visible at every screen width, without needing separate mobile art direction or an `object-fit: contain` letterbox. Updated the `<img>` width/height attributes to 520×780 to match.
- Reviewed the rest of the About page for similar issues: no other cropped faces/hands, no duplicate images, all alt text present, spacing and card alignment consistent. No other changes needed.

## Building-photo removal, About page refresh, and content-accuracy pass

- **Removed the plain building/storefront exterior photo from the entire site**, per explicit owner instruction — it no longer appears anywhere, including the Contact page's "Find Us" section (which now shows address and directions only, no photo). The file itself was not deleted from the project, just removed from display, in case it's wanted again later. The separate photo of two team members waving outside the entrance was intentionally kept (owner confirmed only the plain building shot should go) and remains on the About page's "Meet the Team" section and the Contact page's "Visit Us" section.
- **About page "Our Mission" section**: replaced the building photo with the existing adult CPR hands-on training photo (reused the already-optimized `adult-cpr-skills-practice-nurse-driven-training` asset rather than re-processing a duplicate file), at its native ~3:2 ratio so both students, both mannequins, and the classroom context stay fully visible with no cropping. New alt text: "Nurse Driven Training students practicing adult CPR chest compressions during hands-on classroom instruction."
- This same photo remains on the CPR Certification page's own "Build confidence through hands-on CPR practice" section. No equally strong alternate CPR/classroom photo exists in the project to avoid this one sitewide duplication, so it was intentionally kept on both pages — but presented differently (different heading, copy, crop, and surrounding context) on each, consistent with the site's own "no duplicate use on the same page" rule, which this doesn't violate.
- Updated the homepage's structured-data (JSON-LD) `image` field, which previously pointed at the removed building photo, to the graduation-class photo instead.
- **Removed a standalone, oversized logo block** from the top of the About page (a 260×260px logo sitting alone in its own section between the hero and the Mission copy) — it added little value and delayed getting to real content. The logo remains in the header and footer everywhere.
- **Removed the duplicate graduate-portrait grid from the homepage's "Real Students" section** — the two individual portraits already live in their own "Student Milestones" section on the CNA Training Program page from an earlier round, so showing them again on the homepage was redundant. The homepage now shows only the full graduation-class photo there.
- **Content-accuracy pass**: audited every use of "clinical experience," "clinical exposure," "clinical time," "state exam prepared," and related terms sitewide. Softened all student-facing claims that implied guaranteed clinical placement/hours (which isn't yet arranged — the site is still seeking a sponsor facility) to accurate, verifiable language like "hands-on skills practice" and "instructor-guided practice designed to build confidence." This touched: the homepage's Who We Are checklist and CNA program blurb, the Programs page card copy, the CNA Training Program page's learning outcomes and "Program at a Glance" schedule label, the About page's "How We Teach" card, the FAQ's CNA/HHA comparison and Saturday-schedule and Florida-requirements answers (both the visible accordion and the matching JSON-LD), the Admissions page's schedule list, and the Clinical Partnerships page's "Hands-on trained students" card (which had claimed students complete "clinical hours before certification," directly conflicting with the site's own sponsor-seeking disclosure). Renamed the "Saturday clinicals" schedule label to "Saturday skills lab" everywhere it appeared, since that's a more accurate description of an in-house session absent a confirmed clinical-site partner. Left references to instructors' own real-world clinical/bedside experience as working nurses unchanged — that's a distinct, accurate, and verifiable claim about the teaching staff, not about student clinical placement.
- **CPR Certification page pricing/validity claims** ($75 + $25 registration, 2-year validity, certification card on completion): reviewed and found no invented issuing body or unverifiable certification claim already on the page. These figures could not be independently verified against current business records in this environment — flagging for owner confirmation that pricing and validity terms are still current.
- **Not performed this round (no tool access in this environment)**: an actual Lighthouse run, a production build/lint/type-check pass (not applicable — static HTML/CSS/JS site with no build step), and a live production test-submission of the contact form (submitting a real form entry would notify the actual business inbox, so this was intentionally skipped rather than done without asking first).

## Photo reorganization (homepage About image, CPR/HHA hands-on photos, doorway photo relocation)

- **Homepage "Who We Are" section**: replaced the exterior-doorway photo (two team members waving outside the entrance, in a narrow tall 3:4 crop that cut off the person on the left) with the graduation photo showing a graduate holding her completion certificate alongside program leadership and instructors. New crop uses a natural ~4:3 aspect ratio (matching the source image almost exactly, so all five people — including the woman in the orange dress on the left — stay fully in frame with virtually no cropping). This directly supports the section's mentorship/leadership/completion messaging. New alt text: "Nurse Driven Training graduate holding her completion certificate with program instructors and leadership."
- To avoid using that same photo twice on the homepage, removed it from the "Real Students. Real Skills. Real Progress." three-photo grid further down the page; that grid is now a focused two-photo grid of the two individual graduate portraits.
- **Relocated the exterior-doorway photo** to the Contact page, in a new "Visit Nurse Driven Training" section placed between the contact form and the location/map section. Kept its original portrait crop (safest crop for this source image — minimal cropping) at a moderate contained size. New alt text: "Nurse Driven Training team welcoming students at the Port Charlotte training location." This photo remains in use on the About page's "Meet the Team" section as well, so no image was deleted.
- **CPR Certification page**: split the combined "Real skills practice, not just lecture" two-photo section into two separate, larger sections. Adult CPR chest-compression practice is now its own prominent section right after the course overview/program details ("Build confidence through hands-on CPR practice"), at a wider display size, since it's the strongest hands-on-training image for this page. Infant CPR/pediatric practice now has its own section below it ("Practice skills with guided support").
- Confirmed the infant CPR photo's existing placement on the HHA Program page (in the "Hands-on patient-care and safety skills" section) was already correct per the requested placement logic and needed no change.
- No filenames, compression, or `<picture>`/WebP source sets changed — this was a placement and crop reorganization of existing, already-optimized assets.
- **Audit performed**: cross-checked every `assets/img/...` reference on the edited pages (homepage, Contact, CPR Certification) against actual files in `assets/img/` — zero broken references. Confirmed no image is used more than once on the same page anywhere on the site.
- **Not performed this round (no browser/build tool access in this environment)**: an actual Lighthouse run, a production build/lint/type-check pass (this is a static HTML/CSS/JS site with no build tooling, so these steps don't apply the way they would to a framework-based project), and AVIF generation. Manual checks substituted: broken-link/image sweep (clean), duplicate-image sweep (clean), and a visual review of each source photo at full resolution to choose crop ratios that keep everyone in frame.

## Image repositioning and technical audit

- **Fixed a real layout flaw from the prior round**: the Admissions page combined an individual graduate portrait with the wide graduation-class photo, both forced into a "tall" (3:4) aspect frame — the group photo was visibly cropped/distorted to fit. Admissions now shows only the full graduation-class photo, displayed at its natural wide aspect ratio (photo-frame wide, max-width 900px) with a short supporting paragraph. No portrait remains on this page.
- Relocated both individual graduate portraits to a new "Student Milestones" section on the CNA Training Program page, placed after the classroom/completion content and before the final CTA. Heading: "A moment worth working toward." Both portraits use a consistent, undistorted 4:5 aspect ratio (explicit `aspect-ratio:4/5` matching each image's 340×425 width/height attributes, so no layout shift on load) in a 2-column grid that stacks to a single column below 700px.
- Alt text/captions kept neutral and name-free, as instructed: "Celebrating a Nurse Driven Training program milestone" and "A proud graduation moment at Nurse Driven Training."
- No filenames, compression, or `<picture>`/WebP setup changed — this was a pure repositioning of existing, already-optimized assets.
- **Technical audit performed**: cross-checked every `assets/img/...` reference across all 15 HTML pages against the actual contents of `assets/img/` — zero broken image references sitewide. Verified the new Student Milestones grid collapses to one column on mobile via the existing `.grid-2` responsive rule (`@media (min-width: 700px)`). Verified aspect-ratio consistency on all relocated/repositioned images to prevent layout shift.
- **Not performed this round (no tool access in this environment)**: an actual browser-based Lighthouse run, real Core Web Vitals field data, and AVIF image generation. These remain open follow-ups if wanted; the manual checks above (broken-link/image sweep, CLS/aspect-ratio audit, responsive-grid verification) are the realistic substitute performed instead.

## Real photo integration (graduation, classroom, CPR practice)

- Added 7 authentic photographs supplied by the owner: full CNA graduation-class photo, 2 individual graduate portraits, graduate-with-instructors photo, real classroom instruction photo, adult CPR practice photo, and infant CPR/patient-care practice photo. Renamed with descriptive, SEO-friendly filenames; exported as optimized JPEG + WebP (full-size and mobile variants) with no aggressive filtering, matching the already-polished source images.
- Homepage hero: replaced the older, lower-resolution classroom photo with the new, sharper real classroom instruction photo (same frame/position, updated alt text).
- Homepage: added a new "Real Students. Real Skills. Real Progress." section featuring the graduation-class photo plus both graduate portraits and the graduate-with-instructors photo.
- CNA Training Program page: swapped in the new classroom photo and added a graduation/completion photo section before the final CTA.
- CPR Certification page: added a new "Real skills practice, not just lecture" section with the adult CPR and infant CPR photos side by side.
- HHA Program page: added the infant CPR/patient-care photo to illustrate hands-on safety skills.
- About page: added a "Mentorship that carries through to graduation" section with the graduate-with-instructors photo.
- Admissions page: added a "Students who walked this same path" section with a graduate portrait and the graduation-class photo.
- All new images use `<picture>` with WebP + JPEG sources, `srcset`/`sizes` for mobile-appropriate file sizes, `width`/`height` to prevent layout shift, `loading="lazy"` (except the hero, which stays eager/high-priority), and unique, accurate alt text.
- Not done this round (deferred, out of scope for this pass): a dedicated photo gallery page, per-photo `object-position` tuning at all 5 required breakpoints, a custom 1200×630 Open Graph crop of the graduation photo, and Facility Sponsorship/Clinical Partnerships/Student Resources page placements. All of these are straightforward follow-ups if wanted.

## Contrast and color repairs

- **Fixed a real dark-on-dark bug**: the global `h1, h2, h3, h4 { color: var(--navy); }` rule was overriding the light text color on every dark-background section, making the homepage hero heading ("Start a healthcare career, guided by nurses who've done the work."), the "Partner With Us" banner heading, and every inner-page hero heading nearly invisible against the deep berry background.
- Added a new `--peach-heading` CSS variable (`#F5A897`, a warm coral-peach) and a single component-level override (`.hero`, `.page-hero`, `.cta-banner`, `.section-navy` headings) so every heading on a dark berry background now renders in a clearly readable peach-coral instead of dark berry text.
- Verified contrast mathematically against both ends of the hero/CTA gradient: ~4.9:1 against the lighter `--navy` and ~7.6:1 against the darker `--navy-dark` — both comfortably clear the WCAG AA large-text minimum of 3:1, and clear the normal-text minimum of 4.5:1 as well.
- Confirmed via search that this fix covers every actual instance in the codebase: all `.hero h1` and `.page-hero h1` headings sitewide, and all 12 `.cta-banner h2` CTA headings across every page.

## Image corrections and optimizations

- Enhanced the three real site photographs (`classroom-session.jpg`, `team-welcome.jpg`, `exterior-storefront.jpg`) to fix the washed-out, flat appearance: reclaimed true black/white points (auto-contrast), and applied a modest, tasteful boost to contrast, saturation, and sharpness — deliberately restrained to avoid unnatural skin tones or a heavy color-filter look.
- Re-exported all three as optimized WebP files alongside the originals, and converted every `<img>` reference to these photos (6 places across `index.html`, `about.html` ×2, `cna-training-program.html`, `contact.html`) into a `<picture>` element with a WebP `<source>` and the original JPEG as fallback — smaller file size for supporting browsers with no loss of compatibility.
- Preserved every existing `width`/`height` attribute and `alt` text exactly as before, so there is no layout shift and no accessibility regression from this change.
- Left image crop/aspect ratio handling as-is (`.photo-frame` + `object-fit: cover`), since it was already correctly tuned for each placement.

## Accessibility changes

- Re-verified (did not need to change): exactly one `<h1>` per page across all 15 pages, no duplicate `id` attributes anywhere, every `<img>` has alt text, every `<label for>` has a matching field id, every skip-link target exists, `lang="en"` is present on every page.
- The heading-contrast fix above is itself an accessibility fix (WCAG 1.4.3/1.4.11 contrast).

## UX and conversion changes

- Softened the homepage programs-section heading from "Programs designed to get you certified and job-ready" to "Programs designed to prepare you for certification and a healthcare career" — the original phrasing could be read as promising a guaranteed outcome; the new phrasing accurately describes program intent without implying a guarantee.

## SEO/schema changes

- No schema structure changes were needed this pass; existing Organization/EducationalOrganization, Course, FAQPage, and BreadcrumbList schema were spot-checked and still match visible page content.

## Performance and technical changes

- WebP image variants reduce transferred image weight by roughly 30–35% versus the JPEG originals on supporting browsers, with no fallback risk (JPEG `<img>` remains the fallback source).
- No console-error-prone patterns found (no orphaned label targets, no duplicate IDs, no missing asset references) — verified via a full sitewide link/asset/label audit.

## Re-verified and confirmed clean (no changes needed)

- Full sitewide scan for "accredited program," "state-approved school," "guaranteed certification/employment/exam passage," "nationally recognized," and named certifying bodies — none found. All accreditation-related copy consistently uses the approved transparency language: *"Nurse Driven Training is currently seeking a qualified sponsor facility as part of its pathway toward the applicable program approval and accreditation requirements. We are committed to transparency, educational quality, and full compliance throughout this process."*
- No broken internal links or missing image/script references anywhere on the site.

## Content that still requires owner verification

See `OWNER-VERIFICATION.md` for the full list. Highlights: confirming the accreditation/sponsorship status wording still matches reality at launch time, instructor/founder credentials, next cohort start date, and a real browser-based Lighthouse/accessibility pass (not performed in this environment).
